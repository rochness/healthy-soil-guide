import React, { Component } from 'react';

class Footer extends Component {
  render() {
    /*const actionButtons = ["PARTICIPATE", "FEEDBACK", "DONATE"].map((action) =>
      {
        return (
          <button actionButton
        );
      });*/

    return (
      <div className="footer">
        <h2>FOOTER</h2>
      </div>
    );
  }
}

export default Footer;